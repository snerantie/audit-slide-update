# Technology Management Tower — Meeting Intelligence Agent (POC)
# Python 3.11+ recommended

# AWS SDK for Bedrock
boto3>=1.34.0
botocore>=1.34.0

# Structured output + validation
pydantic>=2.6.0

# Notebook goodies
rich>=13.7.0        # pretty tables/panels for the notebook
ipykernel>=6.29.0   # ensures kernel works in Jupyter/Studio

# Optional: environment variable loading
python-dotenv>=1.0.0

# Optional: for later — document parsing (Agent v2 will need these)
# python-docx>=1.1.0
# pypdf>=4.0.0
